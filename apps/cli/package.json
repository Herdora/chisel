{
    "name": "@chisel/cli",
    "version": "0.1.0",
    "description": "Chisel CLI tool",
    "main": "dist/index.js",
    "bin": {
        "chisel": "./bin/chisel.js"
    },
    "scripts": {
        "build": "tsup src/index.ts --format cjs --dts",
        "dev": "tsup src/index.ts --format cjs --watch",
        "lint": "eslint src --ext .ts"
    },
    "dependencies": {
        "commander": "^11.0.0"
    },
    "devDependencies": {
        "@types/node": "^20.0.0",
        "tsup": "^8.0.0",
        "typescript": "^5.0.0"
    }
}